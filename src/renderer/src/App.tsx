import { Dashboard } from '@/views'

/**
 * Root application component rendering the primary dashboard view.
 * @returns The top-level application element.
 */
function App(): JSX.Element {
  return <Dashboard />
}

export default App
